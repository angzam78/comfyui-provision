version: 1

extensions:
  - module: provisioner_comfyui
    config:
      comfyui_dir: "${WORKSPACE:-/workspace}/ComfyUI"

apt_packages:
  - libsndfile1
  - ffmpeg
  - libgl1
  - libglx-mesa0
  - libglib2.0-0
  - libgomp1

pip_packages:
  packages:
    - opencv-python-headless
    - PyWavelets
    - soundfile
    - imageio-ffmpeg
    - send2trash
    - 'kornia>=0.7.3,<0.8.0'
    - 'llama-cpp-python @ https://github.com/JamePeng/llama-cpp-python/releases/download/v0.3.40-cu128-linux-20260607/llama_cpp_python-0.3.40+cu128-cp312-cp312-linux_x86_64.whl'

git_repos:
  - url: "https://github.com/crystian/ComfyUI-Crystools.git"
    dest: "${WORKSPACE:-/workspace}/ComfyUI/custom_nodes/ComfyUI-Crystools"

  - url: "https://github.com/ltdrdata/ComfyUI-Manager.git"
    dest: "${WORKSPACE:-/workspace}/ComfyUI/custom_nodes/ComfyUI-Manager"

post_commands:
  - |
    mkdir -p /dev/shm/comfyui/{input,output,temp}
    rm -rf ${WORKSPACE:-/workspace}/ComfyUI/{input,output,temp}
    ln -s /dev/shm/comfyui/input  ${WORKSPACE:-/workspace}/ComfyUI/input
    ln -s /dev/shm/comfyui/output ${WORKSPACE:-/workspace}/ComfyUI/output
    ln -s /dev/shm/comfyui/temp   ${WORKSPACE:-/workspace}/ComfyUI/temp
    
  - "/venv/main/bin/python3 ${WORKSPACE:-/workspace}/ComfyUI/custom_nodes/ComfyUI-Manager/cm-cli.py restore-dependencies"
